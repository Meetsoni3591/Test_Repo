# Test Cases for T1.4.3 - Database

Okay, I understand the task. I need to create two detailed test cases related to databases, considering the technologies MySQL, PostgreSQL, and MongoDB. I will focus on common database operations or features.  Here are the two test cases:

**Test Case 1**

*   **Title:** Database Connection and Basic Query Execution - Cross-Database Compatibility

*   **Description:** This test case verifies the ability to successfully connect to different database systems (MySQL, PostgreSQL, and MongoDB) and execute a basic query on each. It aims to ensure the database connections are configured correctly and that the application can interact with each database.

*   **Steps:**

    1.  **Environment Setup:**
        *   Ensure MySQL, PostgreSQL, and MongoDB servers are installed and running.
        *   Create a dedicated test database named "testdb" in each system (MySQL, PostgreSQL, MongoDB).
        *   Create a table named "users" in the MySQL and PostgreSQL databases, with the following schema:
            *   `id` INT PRIMARY KEY AUTO_INCREMENT (MySQL) / SERIAL PRIMARY KEY (PostgreSQL)
            *   `username` VARCHAR(255)
            *   `email` VARCHAR(255)
        *   Insert three sample user records into both the MySQL and PostgreSQL tables (e.g., "user1", "user2", "user3" with corresponding email addresses).
        *   Create a collection named "users" in the MongoDB database, and insert three document of the same users(e.g., "user1", "user2", "user3" with corresponding email addresses).
    2.  **MySQL Connection and Query:**
        *   Using appropriate MySQL client libraries (e.g., `mysql.connector` in Python), establish a connection to the MySQL "testdb" database.
        *   Execute a simple SELECT query: `SELECT * FROM users;`
        *   Retrieve the result set.
        *   Close the MySQL connection.
    3.  **PostgreSQL Connection and Query:**
        *   Using appropriate PostgreSQL client libraries (e.g., `psycopg2` in Python), establish a connection to the PostgreSQL "testdb" database.
        *   Execute a simple SELECT query: `SELECT * FROM users;`
        *   Retrieve the result set.
        *   Close the PostgreSQL connection.
    4.  **MongoDB Connection and Query:**
        *   Using appropriate MongoDB client libraries (e.g., `pymongo` in Python), establish a connection to the MongoDB server.
        *   Access the "testdb" database.
        *   Execute a simple find query: `db.users.find()` (equivalent to SELECT *).
        *   Iterate through the results/documents.
        *   Close the MongoDB connection.
    5.  **Data Validation:**
        *   Confirm result set retrieved from MySQL database.
        *   Confirm result set retrieved from PostgreSQL database.
        *   Confirm result set retrieved from MongoDB database.

*   **Expected Result:**

    *   Successful connections are established to all three database systems (MySQL, PostgreSQL, MongoDB).
    *   The SELECT query (or its MongoDB equivalent) executes without errors on each database.
    *   The retrieved result set contains the expected data (the three inserted user records for MySQL and PostgreSQL, and the three inserted documents for MongoDB). No error messages are generated.

*   **Priority:** High

**Test Case 2**

*   **Title:** Database Data Integrity - Concurrent Update Handling

*   **Description:** This test case evaluates the database system's ability to maintain data integrity when multiple concurrent update operations are performed on the same data.  It focuses on verifying transaction management and preventing data corruption or inconsistencies under high load.

*   **Steps:**

    1.  **Environment Setup:**
        *   Ensure MySQL, PostgreSQL, and MongoDB servers are installed and running.
        *   Create a table named "accounts" in the MySQL and PostgreSQL databases, with the following schema:
            *   `id` INT PRIMARY KEY AUTO_INCREMENT (MySQL) / SERIAL PRIMARY KEY (PostgreSQL)
            *   `account_number` VARCHAR(255) UNIQUE
            *   `balance` DECIMAL(10,2)  (e.g., 1000.00)
        *   Create a collection named "accounts" in the MongoDB database, with the following schema:
            *   `_id`: Object ID
            *   `account_number`: String, unique
            *   `balance`: Number (e.g., 1000.00)
        *   Insert a single account record into each system (MySQL, PostgreSQL, MongoDB) with a specific account number (e.g., "ACC123") and initial balance (e.g., 1000.00).
    2.  **Concurrent Update Simulation:**
        *   Using a multi-threaded or asynchronous programming approach (e.g., using Python's `threading` or `asyncio` modules), simulate multiple concurrent transactions. Each transaction should perform the following:
            *   **MySQL/PostgreSQL:**
                *   Start a transaction.
                *   Read the balance of the account "ACC123".
                *   Subtract a small amount (e.g., 10.00) from the balance.
                *   Update the account balance in the database with the new value.
                *   Commit the transaction.
            *   **MongoDB:**
                *   Read the balance of the account "ACC123".
                *   Subtract a small amount (e.g., 10.00) from the balance.
                *   Update the account balance in the database with the new value using the `update_one` with `$set` operator.
        *   Create a high number of concurrent threads/tasks (e.g., 50-100) to simulate heavy load.
        *   Run the concurrent update operations simultaneously.
    3.  **Data Verification:**
        *   After all concurrent transactions have completed, read the final balance of the account "ACC123" from each database (MySQL, PostgreSQL, MongoDB).
        *   Calculate the expected final balance based on the initial balance and the total amount subtracted by all transactions.

*   **Expected Result:**

    *   The final balance of the account "ACC123" in each database (MySQL, PostgreSQL, and MongoDB) should match the calculated expected balance, considering the number of concurrent updates.
    *   No data corruption or inconsistencies are observed. The database system properly handles concurrent updates and prevents race conditions.
    *   No deadlocks or transaction errors are encountered.

*   **Priority:** High